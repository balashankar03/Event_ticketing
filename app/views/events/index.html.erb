<div style="display: flex; flex-wrap: wrap; gap: 20px; justify-content: center; margin-top: 30px;">
<%= render partial: "events/display", collection: @events, as: :event, locals: { show_controls: false } %>
</div>